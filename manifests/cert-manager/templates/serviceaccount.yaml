apiVersion: v1
kind: ServiceAccount
metadata:
  name: cert-manager
  {{- if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{- else}}
  namespace: cert-manager
  {{- end}}
  labels:
    app: cert-manager
    chart: cert-manager-v0.6.0-dev.0
    release: cert-manager
    heritage: Tiller
